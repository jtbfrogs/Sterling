# ============================================================
# Sterling — Windows Requirements (v2 — RTX 3060 / CUDA)
# Python 3.11+
# Install: pip install -r requirements_windows.txt
#
# Prerequisites before pip install:
#   - CUDA Toolkit 12.x  → https://developer.nvidia.com/cuda-downloads
#   - cuDNN 8.x+         → https://developer.nvidia.com/cudnn
#   - Ollama for Windows → https://ollama.ai
#   - PortAudio          → bundled with PyAudio wheel on Windows (usually fine)
#   - Microsoft C++ Build Tools (for some packages)
# ============================================================

# --- LLM ---
ollama>=0.3.0

# --- Wake Word ---
# No change from Mac — Whisper-based, CPU
openwakeword>=0.6.0
onnx>=1.14.0

# --- Speech to Text ---
# CUDA build — faster-whisper automatically uses GPU when CUDA is present
faster-whisper>=1.0.0

# --- Text to Speech (Offline — replaces Edge-TTS) ---
# Kokoro-82M: high-quality offline TTS, runs fast on RTX 3060
# TODO: pin version once stable Windows build confirmed
# kokoro>=0.9.2
# soundfile>=0.12.0

# Edge-TTS kept as fallback / until Kokoro is wired in
edge-tts>=6.1.0

# --- Audio I/O ---
pyaudio>=0.2.14
numpy>=1.24.0

# --- Vision (HuskyLens2 + future YOLO) ---
pyserial>=3.5
# ultralytics>=8.3.0     # YOLO v11 — uncomment when webcam vision is added

# --- Long-term Memory (planned) ---
# chromadb>=0.5.0
# sentence-transformers>=3.0.0

# --- Web Dashboard (planned) ---
# flask>=3.0.0

# --- Windows Service (planned) ---
# pywin32>=306

# --- Smart Lights ---
# (no change — requests already included)

# --- Spotify ---
spotipy>=2.26.0

# --- Configuration ---
pyyaml>=6.0.0

# --- Utilities ---
requests>=2.31.0
